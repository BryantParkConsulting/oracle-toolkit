# Help docs moved

The `help/` directory previously here was an exact duplicate of `add-in/help/`.

The canonical location is now: [`../add-in/help/`](../add-in/help/)

If you're packaging an installer that ships help content, copy from `add-in/help/`.
