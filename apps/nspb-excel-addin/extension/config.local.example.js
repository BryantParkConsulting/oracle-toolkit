// Copy to config.local.js and set your shared Gemini API key (optional —
// without it, each user enters their own key in the panel's Settings).
window.NSPB_SHARED_GEMINI_KEY = "AIza...your-key-here";
